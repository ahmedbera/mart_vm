import 'package:mart_vm/models/listInfo.dart';

// TODO
/*
  // only in chapter increment
  mu id => \d+: // might be not necessary
  
  // common in all ajax responses
  chapter => c.\d+
    + split by .
  volumes => v.\d+
    + split by .
  
  // only in change list [tested in addtolist]
  otherLists => /option value.* /g
    + foreach => split('>')[1].split('<')[0]

*/